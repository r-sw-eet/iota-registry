# Default points at the live prod API so local dev has real data out of the box.
# Override to http://localhost:3004/api/v1 when running the API locally.
NUXT_PUBLIC_API_BASE=https://iota-trade-scanner.net/api/v1
